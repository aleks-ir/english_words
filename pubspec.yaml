name: words_3000_puzzle
description: A new Flutter project.


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^7.1.0
  get_it: ^7.2.0
  dartz: ^0.10.1
  hive: ^2.0.5
  hive_flutter: ^1.1.0
  http: ^0.13.4

  cupertino_icons: ^1.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  hive_generator: ^1.1.2
  json_serializable: ^6.1.4
  freezed: ^1.1.1
  build_runner: ^2.1.7

flutter:

  uses-material-design: true

  assets:
    - assets/categories/
